import 'package:flutter/material.dart';

class PokemonEvolutionTab extends StatelessWidget {
  const PokemonEvolutionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('EVOLUTION TAB BUILD');

    return const Center(
      child: Text('EVOLUTION TAB'),
    );
  }
}
