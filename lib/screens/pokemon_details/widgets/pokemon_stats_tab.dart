import 'package:flutter/material.dart';

class PokemonStatsTab extends StatelessWidget {
  const PokemonStatsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('STATS TAB BUILD');

    return const Center(
      child: Text('STATS TAB'),
    );
  }
}
